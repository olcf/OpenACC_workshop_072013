This directory contains an MPI parallel code with OpenACC acceleration.

This is mainly to be used to test the CUDA_PROXY (also known as MPI_PROXY)
feature which allows multiple MPI ranks to share the same GPU.

To code is not particularly optimised, but offers the necessary functionality.

